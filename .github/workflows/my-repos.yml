name: Update My Repos

on: workflow_dispatch

jobs:
  my_repos:
    runs-on: ubuntu-latest
    name: It should do something
    steps:
      - name: My Repos
        id: my-repos
        uses: SubZtep/my-repos-action@main
        with:
          # token: ${{ secrets.GH_ACCESS_TOKEN }}
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/checkout@v2
        with:
          persist-credentials: false
          fetch-depth: 0
      - name: Let's README
        shell: bash
        run: |
          cat READI.md > README.md
          echo "${{ steps.my-repos.outputs.table }}" >> README.md
        #run: cat READI.md > README.md && echo $steps.my-repos.outputs.table >> README.md
      - name: Commit files
        run: |
          git config --local user.email "$GITHUB_ACTOR@users.noreply.github.com"
          git config --local user.name "$GITHUB_ACTOR"
          git commit -a -m "Repository changes update"
      - name: Push changes
        uses: ad-m/github-push-action@master
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          branch: ${{ github.head_ref }}
